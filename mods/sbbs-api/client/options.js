


/**
 * Get general ui options to use
 * 
 * Route Handler - Single function statement to use for route
 */
(function getClientOptions(ctx) {
  // TODO: configure web client parameters
  ctx.res.json({
    todo: 'true',
  });
});